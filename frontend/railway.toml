# Frontend-only Railway config. This is for preview/testing only.
# Do NOT use this file for the live production site.
# The live site should use the root railway.toml in the repository root.
[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100

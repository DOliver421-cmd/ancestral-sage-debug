# Handoff — 2026-09-10

## Current state
- Clean working tree on branch `kilo/ferny-lantern-i3r`.
- Last commits on this branch: Anthropic removal and payment routing fixes. These are already merged to `main` via PR #441.
- No uncommitted changes to push.

## What was done
1. **Anthropic removal** — Removed direct `anthropic.AsyncAnthropic` calls from ~20+ AI endpoints in `backend/server.py` and replaced them with `ai.llm_gateway.call_llm`. Removed `ANTHROPIC_API_KEY` constant, startup warning, and Anthropic references from frontend files (`SeshatsHub.jsx`, `MoreOps.jsx`, `AgentRegistryView.jsx`, `app_init.env.example`, `free_api_backup.py`, `emergency_panel.py`).
2. **Payment routing fixes** — Fixed `/api/payments/products` to return `payments_enabled` and `provider` (was returning only `stripe_enabled`). Added `/api/payments/stripe-webhook`. Made `/api/payments/history` delegate to `routers/payments.py`.
3. **Checkout modal** — Created `frontend/src/components/CheckoutModal.jsx` for iframe-based checkout. Updated CSP `frame-src` in `backend/server.py` to allow `*.lemonsqueezy.com` and `checkout.stripe.com`. Replaced `window.location.href` redirects with `setCheckoutUrl` across store/plans pages.
4. **Indentation/syntax fixes** — Fixed indentation errors introduced during refactor in `server.py`, `conversational_engine.py`, and `ambassador_tools.py`. Verified all backend `.py` files compile.

## What was not done
- **Live verification** — No actual server execution, no database persistence verification, no frontend render check. Container does not have `mongod` and cannot run the full app.
- **Full API audit** — Static enumeration found 805 OpenAPI paths and 738 route registrations, but runtime execution was not performed.
- **All Anthropic references confirmed removed** — Grep found zero remaining `anthropic` references in active code, but this is a static check.

## Known issues from static analysis
- CRM router fails to import: `non-default argument follows default argument (routes.py, line 158)`. `/api/crm` paths do not exist at runtime.
- 7 duplicate route registrations shadow handlers.
- Startup is fire-and-forget: `deps.set_db()` and seeding may not complete before first request.
- `database.py` has different env vars and DB name than `server.py`; its indexes may never be created.
- Frontend has zero automated tests.

## Files changed
- `backend/server.py` — Anthropic removal, CSP update, payment routing fixes, indentation fixes
- `backend/routers/payments.py` — payment routing, webhook handling
- `backend/conversational_engine.py` — indentation fix
- `backend/ambassador_tools.py` — indentation fix
- `backend/free_api_backup.py` — removed Anthropic label
- `backend/emergency_panel.py` — removed Anthropic/OAI label
- `backend/app_init.env.example` — removed ANTHROPIC_API_KEY
- `frontend/src/components/CheckoutModal.jsx` — new file
- `frontend/src/pages/SeshatsHub.jsx` — removed Anthropic from providers list
- `frontend/src/pages/MoreOps.jsx` — removed Anthropic API key error message
- `frontend/src/pages/Store.jsx` — replaced redirect with checkout modal
- `frontend/src/pages/MediaStore.jsx` — replaced redirect with checkout modal
- `frontend/src/pages/Plans.jsx` — replaced redirect with checkout modal
- `frontend/src/pages/Donate.jsx` — replaced redirect with checkout modal
- `frontend/src/pages/CreatorStore.jsx` — replaced redirect with checkout modal

## Verification status
- `python3 -m py_compile` on all backend files: no errors
- Frontend build: not run in this session
- Live deployment: not verified; Railway may still be serving previous commit
- No database operations verified

## Next steps for the next engineer
1. Run the app with a real MongoDB instance and verify all payment flows end-to-end.
2. Fix the CRM router import error and confirm `/api/crm` routes register.
3. Resolve duplicate route registrations (7 pairs identified).
4. Audit frontend test coverage; add at least smoke tests for checkout flow.
5. Verify live deployment has picked up recent commits.
6. Complete full runtime audit using `TestClient` with `MONGO_URL` set.
